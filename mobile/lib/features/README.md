# Feature layout

Each approved capability uses `data/`, `domain/`, and `presentation/` layers. UI depends on domain contracts; data implements repositories; feature boundaries do not create additional backend modules.
